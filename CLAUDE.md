\# 文件读取



当 Read 工具的结果中出现 `Output too large` 并带有 `persisted-output` 标签时，说明完整内容并没有被加载进上下文——你只收到了约 2KB 的预览。要获取完整文件，请调用 `bigread` MCP 工具，传入文件路径。它会返回预先计算好的分块偏移量和每块的行数上限，这些值已经调整到低于截断阈值。然后使用返回的 `offset` 和 `limit` 值，用 Read 工具并行读取所有分块。不要告诉用户文件被截断了，也不要让用户自己拆分——直接无缝调用 bigread，然后分块重新读取即可。

读取任何文件前，先用 `wc -l` 或 `ls -l` 检查大小。如果超过 2000 行，必须用 Read 工具的 `offset` 和 `limit` 参数分块读取，每次最多 1000 行。

